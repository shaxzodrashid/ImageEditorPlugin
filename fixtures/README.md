# Fixtures

Tests create minimal deterministic raster stand-ins at runtime. Real PNG/JPEG golden files are
reserved for ImageMagick-enabled CI so this repository does not duplicate opaque binary sources.
